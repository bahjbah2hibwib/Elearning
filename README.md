cách chạy dự án
git clone https://github.com/bahjbah2hibwib/Elearning.git
cd <thư mục clone về>
chạy docker-compose up -d

Backend mở inteliji đợi tải thư viện rồi run

Fontend chạy npm install + npm run dev
